package de.hska.flug.rest.patch

object FlugPatcher {

//    fun patch(flug: Flug, operations: List<PatchOperation>): Flug {
//        val replaceOps = operations.filter { "replace" == it.op }
//        var flugUpdated = replaceOps(flug, replaceOps)
//
//        val addOps = operations.filter { "add" == it.op }
//        flugUpdated = addInteressen(flugUpdated, addOps)
//
//        val removeOps = operations.filter { "remove" == it.op }
//        return removeInteressen(flugUpdated, removeOps)
//    }
}
